import styled from 'styled-components'
import { IAlert } from '../types'

export interface IAlertContainerProps {
  alert: IAlert
}

const StyledAlertContainer = styled.div`
  /* TODO */
`

export default function AlertContainer(props: IAlertContainerProps) {
  return <div>{/* TODO */}</div>
}
