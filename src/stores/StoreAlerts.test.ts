/**
 * @jest-environment jsdom
 */

describe('StoreAlerts', () => {
  it('should add an alert', () => {
    window.localStorage.setItem('toto', 'fu')
    /* TODO */
  })

  it('should add handle event alert messages', () => {
    /* TODO */
  })

  it('should bind the event message', () => {
    /* TODO */
  })

  it('should unbind the event message', () => {
    /* TODO */
  })
})
